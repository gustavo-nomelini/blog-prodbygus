---
interface Props {
  location: string;
  zoom?: number;
}

const { location, zoom = 14 } = Astro.props;
const encodedLocation = encodeURIComponent(location);
---

<div class="w-full h-full">
  <iframe
    width="100%"
    height="100%"
    style="border:0"
    loading="lazy"
    allowfullscreen
    referrerpolicy="no-referrer-when-downgrade"
    src={`https://www.google.com/maps/embed/v1/place?key=${import.meta.env.GOOGLE_MAPS_API_KEY || 'YOUR_API_KEY'}&q=${encodedLocation}&zoom=${zoom}`}
  >
  </iframe>
</div>

<style>
  iframe {
    border-radius: 0.5rem;
  }
</style>

<!-- 
Nota: Para usar este componente em produção:
1. Substitua "YOUR_API_KEY" por uma chave de API válida do Google Maps
2. Ou use uma alternativa como OpenStreetMap que não precisa de API key
--> 