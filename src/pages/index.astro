---
import CyberButton from '../components/CyberButton.jsx';
import Layout from '../layouts/Layout.astro';
---

<Layout 
	title="PRODBYGUS | Desenvolvimento Full Stack" 
	description="Blog pessoal sobre desenvolvimento web, tecnologia e programação"
>
	<!-- Hero Section -->
	<section class="hero-section">
		<div class="container">
			<h1>
				<span class="hero-title">Blog</span>
				<span class="hero-title">PRODBYGUS</span>
			</h1>
			
			<p class="hero-description">
				Compartilhando conhecimentos sobre desenvolvimento Full Stack, tecnologia e soluções para problemas do mundo real.
			</p>
			
			<div class="hero-buttons">
				<CyberButton href="/blog" type="accent" className="pulse-button" client:load>Explorar Posts</CyberButton>
				<CyberButton href="/about" type="secondary" className="glow-button" client:load>
					Sobre Mim
				</CyberButton>
			</div>
		</div>
	</section>
	
	<!-- Services Section -->
	<section class="section-padding relative z-10">
		<div class="mx-auto max-w-7xl px-6 lg:px-8">
			<div class="mx-auto max-w-2xl text-center mb-16">
				<h2 class="text-gradient text-4xl font-semibold tracking-tight relative">
					<span class="relative z-10">Serviços</span>
					<span class="absolute -left-1 -top-1 text-[var(--accent)]/10 blur-sm text-4xl font-semibold tracking-tight">Serviços</span>
					<span class="absolute -right-1 -bottom-1 text-[var(--primary)]/10 blur-sm text-4xl font-semibold tracking-tight">Serviços</span>
				</h2>
				<p class="mt-6 text-lg/8 text-[var(--text-muted)]">Soluções personalizadas para elevar sua presença digital</p>
			</div>
			
			<div class="grid grid-cols-1 md:grid-cols-3 gap-8">
				<!-- Service 1 -->
				<div class="cyberpunk-card">
					<div class="card-glitch-effect"></div>
					<div class="p-6 flex flex-col h-full">
						<div class="mb-5 flex h-16 w-16 items-center justify-center rounded-md bg-[var(--primary)]/20 mx-auto">
							<svg class="h-8 w-8 text-[var(--primary)]" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
								<path stroke-linecap="round" stroke-linejoin="round" d="M12 21a9.004 9.004 0 008.716-6.747M12 21a9.004 9.004 0 01-8.716-6.747M12 21c2.485 0 4.5-4.03 4.5-9S14.485 3 12 3m0 18c-2.485 0-4.5-4.03-4.5-9S9.515 3 12 3m0 0a8.997 8.997 0 017.843 4.582M12 3a8.997 8.997 0 00-7.843 4.582m15.686 0A11.953 11.953 0 0112 10.5c-2.998 0-5.74-1.1-7.843-2.918m15.686 0A8.959 8.959 0 0121 12c0 .778-.099 1.533-.284 2.253m0 0A17.919 17.919 0 0112 16.5c-3.162 0-6.133-.815-8.716-2.247m0 0A9.015 9.015 0 013 12c0-1.605.42-3.113 1.157-4.418" />
							</svg>
						</div>
						<h4 class="mt-5 text-xl font-semibold tracking-tight text-center">Desenvolvimento Web</h4>
						<p class="mt-3 text-[var(--text-muted)] text-center">Sites e aplicações web com foco em usabilidade e performance</p>
					</div>
				</div>
				
				<!-- Service 2 -->
				<div class="cyberpunk-card">
					<div class="card-glitch-effect"></div>
					<div class="p-6 flex flex-col h-full">
						<div class="mb-5 flex h-16 w-16 items-center justify-center rounded-md bg-[var(--primary)]/20 mx-auto">
							<svg class="h-8 w-8 text-[var(--primary)]" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
								<path stroke-linecap="round" stroke-linejoin="round" d="M9 17.25v1.007a3 3 0 01-.879 2.122L7.5 21h9l-.621-.621A3 3 0 0115 18.257V17.25m6-12V15a2.25 2.25 0 01-2.25 2.25H5.25A2.25 2.25 0 013 15V5.25m18 0A2.25 2.25 0 0018.75 3H5.25A2.25 2.25 0 003 5.25m18 0V12a2.25 2.25 0 01-2.25 2.25H5.25A2.25 2.25 0 013 12V5.25" />
							</svg>
						</div>
						<h4 class="mt-5 text-xl font-semibold tracking-tight text-center">Desenvolvimento Full Stack</h4>
						<p class="mt-3 text-[var(--text-muted)] text-center">Aplicações completas com backend e frontend integrados</p>
					</div>
				</div>
				
				<!-- Service 3 -->
				<div class="cyberpunk-card">
					<div class="card-glitch-effect"></div>
					<div class="p-6 flex flex-col h-full">
						<div class="mb-5 flex h-16 w-16 items-center justify-center rounded-md bg-[var(--primary)]/20 mx-auto">
							<svg class="h-8 w-8 text-[var(--primary)]" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
								<path stroke-linecap="round" stroke-linejoin="round" d="M10.5 1.5H8.25A2.25 2.25 0 006 3.75v16.5a2.25 2.25 0 002.25 2.25h7.5A2.25 2.25 0 0018 20.25V3.75a2.25 2.25 0 00-2.25-2.25H13.5m-3 0V3h3V1.5m-3 0h3m-3 18.75h3" />
							</svg>
						</div>
						<h4 class="mt-5 text-xl font-semibold tracking-tight text-center">Consultoria Técnica</h4>
						<p class="mt-3 text-[var(--text-muted)] text-center">Assessoria e melhorias em projetos existentes</p>
					</div>
				</div>
			</div>
			
			<!-- Contact Call-to-action -->
			<div class="mt-16 text-center">
				<h3 class="text-2xl font-bold mb-4 text-gradient">Vamos trabalhar juntos?</h3>
				<p class="text-[var(--text-muted)] max-w-xl mx-auto mb-8">Estou sempre aberto a novos projetos e desafios. Entre em contato para conversarmos sobre como posso ajudar a transformar suas ideias em realidade.</p>
				<CyberButton href="/contact" className="bounce-button" client:load>
					Fale Comigo
				</CyberButton>
			</div>
		</div>
	</section>
</Layout>

<style>
	/* Hero Section */
	.hero-section {
		padding: 8rem 1rem;
		min-height: 85vh;
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
		overflow: hidden;
		z-index: 10;
	}
	
	.container {
		max-width: 64rem;
		margin: 0 auto;
		padding: 0 1.5rem;
		text-align: center;
	}
	
	h1 {
		font-size: 3rem;
		line-height: 1.2;
		font-weight: 800;
		margin-bottom: 2rem;
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}
	
	.hero-title {
		background-image: linear-gradient(45deg, var(--primary), var(--accent));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 100%;
		filter: drop-shadow(0 0 8px rgba(var(--primary-rgb), 0.3));
	}
	
	.hero-description {
		font-size: 1.25rem;
		line-height: 2;
		color: var(--text-muted);
		max-width: 42rem;
		margin: 1.5rem auto 0;
		position: relative;
	}
	
	.hero-description::after {
		content: '';
		position: absolute;
		bottom: -0.5rem;
		left: 0;
		width: 100%;
		height: 2px;
		background: linear-gradient(to right, var(--primary), var(--accent), transparent);
	}
	
	.hero-buttons {
		margin-top: 3rem;
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		justify-content: center;
		gap: 1rem 2rem;
	}
	
	/* Media Queries */
	@media (min-width: 640px) {
		h1 {
			font-size: 3.75rem;
		}
	}
	
	@media (min-width: 1024px) {
		h1 {
			font-size: 4.5rem;
		}
	}
	
	/* Rest of styles */
	.section-padding {
		padding: 6rem 1rem;
	}
	
	.text-gradient {
		background-image: linear-gradient(45deg, var(--primary), var(--accent));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 100%;
		filter: drop-shadow(0 0 8px rgba(var(--primary-rgb), 0.3));
	}
	
	/* Correção para o texto da hero section */
	.glitch-text {
		position: relative;
		z-index: 1;
		display: block;
	}
	
	.glitch-text span {
		display: block;
	}
	
	.glitch-text .absolute {
		z-index: 0;
		pointer-events: none;
	}
	
	.cyberpunk-card {
		position: relative;
		overflow: hidden;
		background-color: var(--surface);
		border: 1px solid var(--primary);
		border-radius: 0.375rem;
		box-shadow: 0 0 20px rgba(var(--primary-rgb), 0.1);
		transition: all 0.3s ease;
	}
	
	.cyberpunk-card:hover {
		transform: translateY(-5px);
		box-shadow: 0 10px 25px rgba(var(--primary-rgb), 0.2);
		border-color: var(--primary);
	}
	
	.cyberpunk-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 3px;
		background: linear-gradient(90deg, var(--primary), var(--accent));
		opacity: 0.7;
	}
	
	.card-glitch-effect {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			45deg, 
			rgba(var(--primary-rgb), 0) 0%, 
			rgba(var(--primary-rgb), 0.05) 25%, 
			rgba(var(--primary-rgb), 0) 50%, 
			rgba(var(--accent-rgb), 0.05) 75%, 
			rgba(var(--accent-rgb), 0) 100%
		);
		background-size: 200% 200%;
		animation: glitch-animation 5s linear infinite;
		pointer-events: none;
		opacity: 0;
		transition: opacity 0.3s ease;
	}
	
	.cyberpunk-card:hover .card-glitch-effect {
		opacity: 1;
	}
	
	@keyframes glitch-animation {
		0% {
			background-position: 0% 0%;
		}
		100% {
			background-position: 200% 200%;
		}
	}
	
	@media (min-width: 768px) {
		.section-padding {
			padding: 8rem 2rem;
		}
	}
	
	/* Estilos dos botões aprimorados */
	:global(.pulse-button) {
		animation: pulse 2s infinite;
		box-shadow: 0 0 0 0 rgba(var(--accent-rgb), 0.7);
	}
	
	@keyframes pulse {
		0% {
			box-shadow: 0 0 0 0 rgba(var(--accent-rgb), 0.7);
		}
		70% {
			box-shadow: 0 0 0 10px rgba(var(--accent-rgb), 0);
		}
		100% {
			box-shadow: 0 0 0 0 rgba(var(--accent-rgb), 0);
		}
	}
	
	:global(.glow-button) {
		position: relative;
		overflow: hidden;
	}
	
	:global(.glow-button)::after {
		content: '';
		position: absolute;
		top: -50%;
		left: -50%;
		width: 200%;
		height: 200%;
		background: linear-gradient(
			to right,
			rgba(var(--primary-rgb), 0) 0%,
			rgba(var(--primary-rgb), 0.1) 50%,
			rgba(var(--primary-rgb), 0) 100%
		);
		transform: rotate(30deg);
		animation: glow 3s infinite linear;
		opacity: 0;
		transition: opacity 0.3s ease;
	}
	
	:global(.glow-button:hover)::after {
		opacity: 1;
	}
	
	@keyframes glow {
		0% {
			transform: translateX(-100%) rotate(30deg);
		}
		100% {
			transform: translateX(100%) rotate(30deg);
		}
	}
	
	:global(.bounce-button) {
		transition: transform 0.3s ease;
	}
	
	:global(.bounce-button:hover) {
		animation: bounce 1s infinite;
	}
	
	@keyframes bounce {
		0%, 100% {
			transform: translateY(0);
		}
		50% {
			transform: translateY(-5px);
		}
	}
</style>
