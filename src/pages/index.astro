---
import CyberButton from '../components/CyberButton.jsx';
import Layout from '../layouts/Layout.astro';
---

<Layout 
	title="Blog | PRODBYGUS" 
	description="Blog pessoal sobre desenvolvimento web, tecnologia e programação"
>
	<!-- Hero Section -->
	<section class="hero-section">
		<div class="container">
			<div class="cyberpunk-card">
				<div class="card-glitch-effect"></div>
				<div class="circuit-overlay"></div>
				<div class="scanline"></div>
				<div class="corner-tl"></div>
				<div class="corner-tr"></div>
				<div class="corner-bl"></div>
				<div class="corner-br"></div>
				<div class="mobile-signal"></div>
				<div class="p-5 sm:p-8 md:p-10">
					<h1 class="unified-title-container">
						<div class="unified-title-wrapper">
							<span class="hero-title" data-text="Blog">Blog</span>
							<span class="hero-title" data-text="PRODBYGUS">PRODBYGUS</span>
							<div class="title-glitch-overlay"></div>
						</div>
					</h1>
					
					<p class="hero-description">
						Compartilhando conhecimentos sobre desenvolvimento Full Stack, tecnologia e soluções para problemas do mundo real.
					</p>
					
					<div class="hero-buttons">
						<CyberButton href="/blog" type="accent" className="pulse-button" client:load>Explorar Posts</CyberButton>
						<CyberButton href="/about" type="secondary" className="glow-button" client:load>
							Sobre Mim
						</CyberButton>
					</div>
				</div>
			</div>
		</div>
	</section>
	
	<!-- Services Section -->
	<section class="section-padding relative z-10">
		<div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
			<div class="mx-auto max-w-2xl text-center mb-10 sm:mb-16">
				<h2 class="text-gradient text-3xl sm:text-4xl font-semibold tracking-tight relative">
					<span class="relative z-10">Serviços</span>
					<span class="absolute -left-1 -top-1 text-[var(--accent)]/10 blur-sm text-3xl sm:text-4xl font-semibold tracking-tight">Serviços</span>
					<span class="absolute -right-1 -bottom-1 text-[var(--primary)]/10 blur-sm text-3xl sm:text-4xl font-semibold tracking-tight">Serviços</span>
				</h2>
				<p class="mt-4 sm:mt-6 text-base sm:text-lg/8 text-[var(--text-muted)]">Soluções personalizadas para elevar sua presença digital</p>
			</div>
			
			<div class="grid grid-cols-1 gap-6 sm:gap-8 md:grid-cols-3">
				<!-- Service 1 -->
				<div class="cyberpunk-card">
					<div class="card-glitch-effect"></div>
					<div class="p-5 sm:p-6 flex flex-col h-full">
						<div class="mb-4 sm:mb-5 flex h-14 sm:h-16 w-14 sm:w-16 items-center justify-center rounded-md bg-[var(--primary)]/20 mx-auto">
							<svg class="h-7 w-7 sm:h-8 sm:w-8 text-[var(--primary)]" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
								<path stroke-linecap="round" stroke-linejoin="round" d="M12 21a9.004 9.004 0 008.716-6.747M12 21a9.004 9.004 0 01-8.716-6.747M12 21c2.485 0 4.5-4.03 4.5-9S14.485 3 12 3m0 18c-2.485 0-4.5-4.03-4.5-9S9.515 3 12 3m0 0a8.997 8.997 0 017.843 4.582M12 3a8.997 8.997 0 00-7.843 4.582m15.686 0A11.953 11.953 0 0112 10.5c-2.998 0-5.74-1.1-7.843-2.918m15.686 0A8.959 8.959 0 0121 12c0 .778-.099 1.533-.284 2.253m0 0A17.919 17.919 0 0112 16.5c-3.162 0-6.133-.815-8.716-2.247m0 0A9.015 9.015 0 013 12c0-1.605.42-3.113 1.157-4.418" />
							</svg>
						</div>
						<h4 class="mt-4 sm:mt-5 text-lg sm:text-xl font-semibold tracking-tight text-center">Desenvolvimento Web</h4>
						<p class="mt-2 sm:mt-3 text-sm sm:text-base text-[var(--text-muted)] text-center">Sites e aplicações web com foco em usabilidade e performance</p>
					</div>
				</div>
				
				<!-- Service 2 -->
				<div class="cyberpunk-card">
					<div class="card-glitch-effect"></div>
					<div class="p-5 sm:p-6 flex flex-col h-full">
						<div class="mb-4 sm:mb-5 flex h-14 sm:h-16 w-14 sm:w-16 items-center justify-center rounded-md bg-[var(--primary)]/20 mx-auto">
							<svg class="h-7 w-7 sm:h-8 sm:w-8 text-[var(--primary)]" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
								<path stroke-linecap="round" stroke-linejoin="round" d="M9 17.25v1.007a3 3 0 01-.879 2.122L7.5 21h9l-.621-.621A3 3 0 0115 18.257V17.25m6-12V15a2.25 2.25 0 01-2.25 2.25H5.25A2.25 2.25 0 013 15V5.25m18 0A2.25 2.25 0 0018.75 3H5.25A2.25 2.25 0 003 5.25m18 0V12a2.25 2.25 0 01-2.25 2.25H5.25A2.25 2.25 0 013 12V5.25" />
							</svg>
						</div>
						<h4 class="mt-4 sm:mt-5 text-lg sm:text-xl font-semibold tracking-tight text-center">Desenvolvimento Full Stack</h4>
						<p class="mt-2 sm:mt-3 text-sm sm:text-base text-[var(--text-muted)] text-center">Aplicações completas com backend e frontend integrados</p>
					</div>
				</div>
				
				<!-- Service 3 -->
				<div class="cyberpunk-card">
					<div class="card-glitch-effect"></div>
					<div class="p-5 sm:p-6 flex flex-col h-full">
						<div class="mb-4 sm:mb-5 flex h-14 sm:h-16 w-14 sm:w-16 items-center justify-center rounded-md bg-[var(--primary)]/20 mx-auto">
							<svg class="h-7 w-7 sm:h-8 sm:w-8 text-[var(--primary)]" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
								<path stroke-linecap="round" stroke-linejoin="round" d="M10.5 1.5H8.25A2.25 2.25 0 006 3.75v16.5a2.25 2.25 0 002.25 2.25h7.5A2.25 2.25 0 0018 20.25V3.75a2.25 2.25 0 00-2.25-2.25H13.5m-3 0V3h3V1.5m-3 0h3m-3 18.75h3" />
							</svg>
						</div>
						<h4 class="mt-4 sm:mt-5 text-lg sm:text-xl font-semibold tracking-tight text-center">Consultoria Técnica</h4>
						<p class="mt-2 sm:mt-3 text-sm sm:text-base text-[var(--text-muted)] text-center">Assessoria e melhorias em projetos existentes</p>
					</div>
				</div>
			</div>
			
			<!-- Contact Call-to-action -->
			<div class="mt-12 sm:mt-16 text-center">
				<h3 class="text-xl sm:text-2xl font-bold mb-3 sm:mb-4 text-gradient">Vamos trabalhar juntos?</h3>
				<p class="text-sm sm:text-base text-[var(--text-muted)] max-w-xl mx-auto mb-6 sm:mb-8">Estou sempre aberto a novos projetos e desafios. Entre em contato para conversarmos sobre como posso ajudar a transformar suas ideias em realidade.</p>
				<CyberButton href="/contact" className="bounce-button" client:load>
					Fale Comigo
				</CyberButton>
			</div>
		</div>
	</section>
</Layout>

<style>
	/* Hero Section */
	.hero-section {
		padding: 4rem 1rem 2rem;
		min-height: 60vh;
		display: flex;
		align-items: center;
		justify-content: center;
		position: relative;
		overflow: hidden;
		z-index: 10;
		background-color: rgba(0, 0, 0, 0.2);
		box-shadow: inset 0 0 50px rgba(0, 0, 0, 0.3);
	}
	
	@media (min-width: 768px) {
		.hero-section {
			padding: 6rem 1rem 4rem;
			min-height: 80vh;
		}
	}
	
	.hero-section::after {
		content: '';
		position: absolute;
		bottom: 0;
		left: 0;
		width: 100%;
		height: 5px;
		background: linear-gradient(90deg, transparent, var(--primary), var(--accent), var(--primary), transparent);
		box-shadow: 0 0 15px 2px rgba(var(--accent-rgb), 0.6);
		z-index: 1;
	}
	
	.container {
		max-width: 64rem;
		margin: 0 auto;
		padding: 0 0.75rem;
		text-align: center;
		width: 100%;
	}
	
	@media (min-width: 640px) {
		.container {
			padding: 0 1.5rem;
		}
	}
	
	/* Cyberpunk Card Enhanced */
	.cyberpunk-card {
		position: relative;
		overflow: hidden;
		background-color: rgba(var(--surface-rgb), 0.8);
		border: 1px solid var(--primary);
		border-radius: 0.5rem;
		box-shadow: 0 0 20px rgba(var(--primary-rgb), 0.1);
		transition: all 0.3s cubic-bezier(0.17, 0.67, 0.83, 0.67);
		backdrop-filter: blur(10px);
		transform-style: preserve-3d;
		perspective: 1000px;
		cursor: pointer;
		width: 100%;
	}
	
	@media (max-width: 640px) {
		.cyberpunk-card {
			border-width: 2px;
			background-color: rgba(var(--surface-rgb), 0.9);
		}
	}
	
	.cyberpunk-card:hover {
		transform: translateY(-5px) scale(1.02);
		box-shadow: 
			0 10px 25px rgba(var(--primary-rgb), 0.2),
			0 0 0 2px rgba(var(--primary-rgb), 0.4),
			0 0 20px rgba(var(--accent-rgb), 0.3);
		border-color: var(--accent);
	}
	
	/* Disable hover effects on mobile for better performance */
	@media (max-width: 640px) {
		.cyberpunk-card:hover {
			transform: none;
			box-shadow: 
				0 5px 15px rgba(var(--primary-rgb), 0.2),
				0 0 0 2px rgba(var(--primary-rgb), 0.4),
				0 0 10px rgba(var(--accent-rgb), 0.3);
		}
	}
	
	.cyberpunk-card:active {
		transform: translateY(0) scale(0.98);
		box-shadow: 
			0 5px 15px rgba(var(--primary-rgb), 0.1),
			0 0 0 1px rgba(var(--primary-rgb), 0.2),
			0 0 10px rgba(var(--accent-rgb), 0.2);
		transition: all 0.1s;
	}
	
	.cyberpunk-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 3px;
		background: linear-gradient(90deg, var(--primary), var(--accent));
		opacity: 0.7;
		z-index: 2;
		transition: all 0.3s ease;
	}
	
	.cyberpunk-card:hover::before {
		height: 5px;
		box-shadow: 0 0 15px 2px rgba(var(--accent-rgb), 0.8);
		animation: neonFlicker 1.5s infinite alternate;
	}
	
	@keyframes neonFlicker {
		0%, 19%, 21%, 23%, 25%, 54%, 56%, 100% {
			opacity: 0.9;
			box-shadow: 0 0 15px 2px rgba(var(--accent-rgb), 0.8);
		}
		20%, 24%, 55% {
			opacity: 0.5;
			box-shadow: 0 0 5px 1px rgba(var(--accent-rgb), 0.4);
		}
	}
	
	/* Card Glitch Effect */
	.card-glitch-effect {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			45deg, 
			rgba(var(--primary-rgb), 0) 0%, 
			rgba(var(--primary-rgb), 0.05) 25%, 
			rgba(var(--primary-rgb), 0) 50%, 
			rgba(var(--accent-rgb), 0.05) 75%, 
			rgba(var(--accent-rgb), 0) 100%
		);
		background-size: 200% 200%;
		animation: glitch-animation 5s linear infinite;
		pointer-events: none;
		opacity: 0;
		z-index: 1;
		transition: opacity 0.3s ease;
	}
	
	.cyberpunk-card:hover .card-glitch-effect {
		opacity: 1;
		animation: glitch-animation 3s linear infinite;
	}
	
	@keyframes glitch-animation {
		0% {
			background-position: 0% 0%;
		}
		100% {
			background-position: 200% 200%;
		}
	}
	
	/* Circuit Overlay */
	.circuit-overlay {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background-image: url("data:image/svg+xml,%3Csvg width='100' height='100' viewBox='0 0 100 100' xmlns='http://www.w3.org/2000/svg'%3E%3Cpath d='M10 10h5v5h-5zM20 10h5v5h-5zM30 10h5v5h-5zM10 20h5v5h-5zM30 20h5v5h-5zM50 20h5v5h-5zM70 20h5v5h-5zM90 20h5v5h-5zM10 30h5v5h-5zM30 30h5v5h-5zM50 30h5v5h-5zM70 30h5v5h-5zM90 30h5v5h-5zM10 40h5v5h-5zM30 40h5v5h-5zM50 40h5v5h-5zM70 40h5v5h-5zM90 40h5v5h-5zM10 50h5v5h-5zM30 50h5v5h-5zM50 50h5v5h-5zM70 50h5v5h-5zM10 60h5v5h-5zM30 60h5v5h-5zM50 60h5v5h-5zM70 60h5v5h-5zM90 60h5v5h-5zM10 70h5v5h-5zM30 70h5v5h-5zM50 70h5v5h-5zM70 70h5v5h-5zM90 70h5v5h-5zM10 80h5v5h-5zM30 80h5v5h-5zM50 80h5v5h-5zM70 80h5v5h-5zM90 80h5v5h-5zM10 90h5v5h-5zM30 90h5v5h-5zM50 90h5v5h-5zM70 90h5v5h-5zM90 90h5v5h-5z' fill='rgba(var(--primary-rgb), 0.1)' fill-rule='evenodd'/%3E%3C/svg%3E");
		opacity: 0;
		transition: opacity 0.8s ease;
		z-index: 1;
		mix-blend-mode: overlay;
		pointer-events: none;
	}
	
	.cyberpunk-card:hover .circuit-overlay {
		opacity: 0.4;
		animation: circuit-fade 4s infinite alternate;
	}
	
	@keyframes circuit-fade {
		0% {
			opacity: 0.2;
		}
		50% {
			opacity: 0.4;
		}
		100% {
			opacity: 0.3;
		}
	}
	
	/* Scanline effect */
	.scanline {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			to bottom,
			transparent 50%,
			rgba(var(--accent-rgb), 0.02) 51%,
			rgba(var(--accent-rgb), 0.02) 51.5%,
			transparent 52%
		);
		background-size: 100% 4px;
		opacity: 0;
		transition: opacity 0.3s ease;
		z-index: 2;
		pointer-events: none;
	}
	
	.cyberpunk-card:hover .scanline {
		opacity: 1;
		animation: scanline 6s linear infinite;
	}
	
	@keyframes scanline {
		0% {
			background-position: 0 0;
		}
		100% {
			background-position: 0 100%;
		}
	}
	
	/* Cyberpunk corners */
	.corner-tl, .corner-tr, .corner-bl, .corner-br {
		position: absolute;
		width: 15px;
		height: 15px;
		z-index: 3;
		transition: all 0.3s ease;
		opacity: 0.7;
	}
	
	.corner-tl {
		top: 0;
		left: 0;
		border-top: 2px solid var(--primary);
		border-left: 2px solid var(--primary);
	}
	
	.corner-tr {
		top: 0;
		right: 0;
		border-top: 2px solid var(--primary);
		border-right: 2px solid var(--primary);
	}
	
	.corner-bl {
		bottom: 0;
		left: 0;
		border-bottom: 2px solid var(--primary);
		border-left: 2px solid var(--primary);
	}
	
	.corner-br {
		bottom: 0;
		right: 0;
		border-bottom: 2px solid var(--primary);
		border-right: 2px solid var(--primary);
	}
	
	.cyberpunk-card:hover .corner-tl,
	.cyberpunk-card:hover .corner-tr,
	.cyberpunk-card:hover .corner-bl,
	.cyberpunk-card:hover .corner-br {
		width: 20px;
		height: 20px;
		opacity: 1;
		border-color: var(--accent);
		box-shadow: 0 0 8px rgba(var(--accent-rgb), 0.7);
	}
	
	/* Hero Section Heading - Unified Animation */
	.hero-section h1 {
		position: relative;
		isolation: isolate;
		padding: 0.25rem;
		margin-bottom: 1rem;
		perspective: 1000px;
	}
	
	@media (min-width: 640px) {
		.hero-section h1 {
			margin-bottom: 1.5rem;
			padding: 0.5rem;
		}
	}
	
	@media (min-width: 1024px) {
		.hero-section h1 {
			margin-bottom: 2rem;
		}
	}
	
	.unified-title-container {
		display: flex;
		justify-content: center;
		align-items: center;
		position: relative;
		z-index: 1;
	}
	
	.unified-title-wrapper {
		display: flex;
		flex-direction: column;
		gap: 0.25rem;
		position: relative;
		transition: all 0.3s ease;
		transform-style: preserve-3d;
		cursor: pointer;
		padding: 0.5rem;
	}
	
	@media (min-width: 640px) {
		.unified-title-wrapper {
			gap: 0.5rem;
		}
	}
	
	.unified-title-wrapper:hover {
		transform: translateZ(5px) scale(1.05);
		filter: drop-shadow(0 0 20px rgba(var(--accent-rgb), 0.8));
		animation: unified-glitch 0.4s linear 2;
	}
	
	.unified-title-wrapper::before {
		content: '';
		position: absolute;
		top: -10px;
		left: -10px;
		right: -10px;
		bottom: -10px;
		border: 2px solid rgba(var(--primary-rgb), 0.8);
		filter: blur(2px);
		opacity: 0.6;
		animation: borderPulse 4s infinite alternate;
		z-index: -1;
		border-radius: 5px;
	}
	
	/* Title Glitch Overlay */
	.title-glitch-overlay {
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		bottom: 0;
		background: linear-gradient(
			45deg, 
			rgba(var(--primary-rgb), 0) 0%, 
			rgba(var(--primary-rgb), 0.05) 25%, 
			rgba(var(--primary-rgb), 0) 50%, 
			rgba(var(--accent-rgb), 0.05) 75%, 
			rgba(var(--accent-rgb), 0) 100%
		);
		background-size: 200% 200%;
		z-index: 10;
		opacity: 0;
		mix-blend-mode: overlay;
		pointer-events: none;
		transition: opacity 0.3s ease;
	}
	
	.unified-title-wrapper:hover .title-glitch-overlay {
		opacity: 0.7;
		animation: title-glitch-animation 3s linear infinite;
	}
	
	@keyframes title-glitch-animation {
		0% {
			background-position: 0% 0%;
		}
		100% {
			background-position: 200% 200%;
		}
	}
	
	@keyframes unified-glitch {
		0% {
			transform: translateZ(5px) scale(1.05);
			filter: drop-shadow(0 0 20px rgba(var(--accent-rgb), 0.8));
		}
		25% {
			transform: translate(4px, 0) translateZ(5px) scale(1.05);
			filter: drop-shadow(-4px 0 20px rgba(var(--primary-rgb), 0.8));
		}
		50% {
			transform: translate(-2px, 2px) translateZ(5px) scale(1.05);
			filter: drop-shadow(2px -2px 20px rgba(var(--accent-rgb), 0.8));
		}
		75% {
			transform: translate(2px, -2px) translateZ(5px) scale(1.05);
			filter: drop-shadow(-2px 2px 20px rgba(var(--primary-rgb), 0.8));
		}
		100% {
			transform: translateZ(5px) scale(1.05);
			filter: drop-shadow(0 0 20px rgba(var(--accent-rgb), 0.8));
		}
	}
	
	/* Hero Title with Glitch Hover Effect */
	.hero-title {
		position: relative;
		display: inline-block;
		background-image: linear-gradient(45deg, 
			rgba(var(--primary-rgb), 1), 
			rgba(var(--accent-rgb), 1));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 100%;
		filter: drop-shadow(0 0 8px rgba(var(--primary-rgb), 0.6));
		text-shadow: 
			0 0 4px rgba(0, 0, 0, 1),
			0 0 8px rgba(var(--primary-rgb), 0.7),
			0 0 1px rgba(255, 255, 255, 0.9);
		animation: pulse-title 4s infinite alternate;
		transform-style: preserve-3d;
		transform: translateZ(0);
		text-transform: uppercase;
		letter-spacing: 2px;
		font-weight: 900;
		transition: all 0.3s ease;
		font-size: 2rem;
		line-height: 1.2;
	}
	
	@media (min-width: 640px) {
		.hero-title {
			font-size: 2.5rem;
		}
	}
	
	@media (min-width: 1024px) {
		.hero-title {
			font-size: 4rem;
		}
	}
	
	.hero-title::before {
		content: attr(data-text);
		position: absolute;
		left: 0;
		top: 0;
		width: 100%;
		height: 100%;
		color: transparent;
		-webkit-text-stroke: 5px rgba(0, 0, 0, 0.9);
		z-index: -4;
	}
	
	.hero-title::after {
		content: attr(data-text);
		position: absolute;
		left: 0;
		top: 0;
		width: 100%;
		height: 100%;
		color: transparent;
		-webkit-text-stroke: 3px rgba(255, 255, 255, 1);
		filter: blur(0);
		z-index: -2;
		animation: subtle-glitch 5s infinite alternate;
	}
	
	.hero-title span {
		position: absolute;
		left: 0;
		top: 0;
		width: 100%;
		height: 100%;
		color: transparent;
		-webkit-text-stroke: 1px rgba(255, 255, 255, 1);
		z-index: -1;
	}
	
	@keyframes subtle-glitch {
		0% {
			transform: translate(0, 0) translateZ(-5px);
		}
		25% {
			transform: translate(0.5px, -0.5px) translateZ(-5px);
		}
		50% {
			transform: translate(-0.5px, -0.5px) translateZ(-5px);
		}
		75% {
			transform: translate(0.5px, 0.5px) translateZ(-5px);
		}
		100% {
			transform: translate(0, 0) translateZ(-5px);
		}
	}
	
	@keyframes pulse-title {
		0% {
			filter: drop-shadow(0 0 8px rgba(var(--primary-rgb), 0.7));
		}
		100% {
			filter: drop-shadow(0 0 12px rgba(var(--accent-rgb), 0.7));
		}
	}
	
	/* Hero Description */
	.hero-description {
		font-size: 0.875rem;
		line-height: 1.6;
		color: var(--text-muted);
		max-width: 42rem;
		margin: 1rem auto 0;
		position: relative;
		transition: all 0.3s ease;
	}
	
	.cyberpunk-card:hover .hero-description {
		text-shadow: 0 0 2px rgba(var(--accent-rgb), 0.3);
	}
	
	@media (min-width: 640px) {
		.hero-description {
			font-size: 1.125rem;
			line-height: 1.8;
			margin: 1.5rem auto 0;
		}
	}
	
	@media (min-width: 1024px) {
		.hero-description {
			font-size: 1.25rem;
			line-height: 2;
		}
	}
	
	.hero-description::after {
		content: '';
		position: absolute;
		bottom: -0.5rem;
		left: 0;
		width: 100%;
		height: 2px;
		background: linear-gradient(to right, var(--primary), var(--accent), transparent);
		transition: all 0.3s ease;
	}
	
	.cyberpunk-card:hover .hero-description::after {
		height: 3px;
		box-shadow: 0 0 8px rgba(var(--accent-rgb), 0.5);
	}
	
	.hero-buttons {
		margin-top: 1.5rem;
		display: flex;
		flex-direction: column;
		align-items: center;
		justify-content: center;
		gap: 0.75rem;
		position: relative;
		z-index: 5;
	}
	
	@media (min-width: 640px) {
		.hero-buttons {
			margin-top: 2.5rem;
			flex-direction: row;
			flex-wrap: wrap;
			gap: 1rem 2rem;
		}
	}
	
	/* Mobile signal indicator - only visible on mobile */
	.mobile-signal {
		position: absolute;
		top: 10px;
		right: 10px;
		width: 20px;
		height: 20px;
		border-radius: 50%;
		background: var(--accent);
		box-shadow: 0 0 10px rgba(var(--accent-rgb), 0.7);
		z-index: 5;
		opacity: 0;
		animation: signal-pulse 2s infinite;
	}
	
	@media (max-width: 640px) {
		.mobile-signal {
			opacity: 1;
		}
	}
	
	@keyframes signal-pulse {
		0%, 100% {
			opacity: 1;
			box-shadow: 0 0 10px rgba(var(--accent-rgb), 0.7);
		}
		50% {
			opacity: 0.5;
			box-shadow: 0 0 5px rgba(var(--accent-rgb), 0.3);
		}
	}
	
	/* Rest of styles */
	.section-padding {
		padding: 4rem 1rem;
		position: relative;
	}
	
	/* Separador de seção futurista */
	.section-padding::before {
		content: '';
		position: absolute;
		top: 0;
		left: 50%;
		transform: translateX(-50%);
		width: 80%;
		max-width: 1200px;
		height: 1px;
		background: linear-gradient(90deg, transparent, var(--accent), transparent);
		opacity: 0.7;
	}
	
	/* Elemento decorativo cyberpunk nas laterais */
	.section-padding::after {
		content: '';
		position: absolute;
		bottom: 15px;
		left: 0;
		width: 100%;
		height: 8px;
		background-image: repeating-linear-gradient(
			90deg,
			transparent,
			transparent 15px,
			var(--primary) 15px,
			var(--primary) 20px
		);
		opacity: 0.3;
		z-index: -1;
	}
	
	.text-gradient {
		background-image: linear-gradient(45deg, var(--primary), var(--accent));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 100%;
		filter: drop-shadow(0 0 8px rgba(var(--primary-rgb), 0.3));
	}
	
	/* Correção para o texto da hero section */
	.glitch-text {
		position: relative;
		z-index: 1;
		display: block;
	}
	
	.glitch-text span {
		display: block;
	}
	
	.glitch-text .absolute {
		z-index: 0;
		pointer-events: none;
	}
	
	@keyframes borderPulse {
		0% {
			opacity: 0.4;
			filter: blur(2px);
			border-color: rgba(var(--primary-rgb), 0.7);
		}
		100% {
			opacity: 0.6;
			filter: blur(1px);
			border-color: rgba(var(--accent-rgb), 0.7);
		}
	}
	
	/* Contact section enhancement */
	.section-padding .mt-16 {
		position: relative;
		background: linear-gradient(rgba(var(--surface-rgb), 0.7), rgba(var(--surface-rgb), 0.7));
		padding: 2rem;
		border-radius: 0.5rem;
		border-left: 2px solid var(--primary);
		border-right: 2px solid var(--accent);
		box-shadow: 0 8px 25px rgba(var(--accent-rgb), 0.05);
		backdrop-filter: blur(5px);
		margin-top: 3rem;
	}
	
	.section-padding .mt-16::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		width: 100%;
		height: 2px;
		background: linear-gradient(90deg, var(--primary), var(--accent));
	}
	
	.section-padding .mt-16::after {
		content: '';
		position: absolute;
		bottom: 0;
		left: 0;
		width: 100%;
		height: 2px;
		background: linear-gradient(90deg, var(--accent), var(--primary));
	}
	
	/* Estilos dos botões aprimorados */
	:global(.pulse-button) {
		animation: pulse 2s infinite;
		box-shadow: 0 0 0 0 rgba(var(--accent-rgb), 0.7);
	}
	
	@keyframes pulse {
		0% {
			box-shadow: 0 0 0 0 rgba(var(--accent-rgb), 0.7);
		}
		70% {
			box-shadow: 0 0 0 10px rgba(var(--accent-rgb), 0);
		}
		100% {
			box-shadow: 0 0 0 0 rgba(var(--accent-rgb), 0);
		}
	}
	
	:global(.glow-button) {
		position: relative;
		overflow: hidden;
	}
	
	:global(.glow-button)::after {
		content: '';
		position: absolute;
		top: -50%;
		left: -50%;
		width: 200%;
		height: 200%;
		background: linear-gradient(
			to right,
			rgba(var(--primary-rgb), 0) 0%,
			rgba(var(--primary-rgb), 0.1) 50%,
			rgba(var(--primary-rgb), 0) 100%
		);
		transform: rotate(30deg);
		animation: glow 3s infinite linear;
		opacity: 0;
		transition: opacity 0.3s ease;
	}
	
	:global(.glow-button:hover)::after {
		opacity: 1;
	}
	
	@keyframes glow {
		0% {
			transform: translateX(-100%) rotate(30deg);
		}
		100% {
			transform: translateX(100%) rotate(30deg);
		}
	}
	
	:global(.bounce-button) {
		transition: transform 0.3s ease;
	}
	
	:global(.bounce-button:hover) {
		animation: bounce 1s infinite;
	}
	
	@keyframes bounce {
		0%, 100% {
			transform: translateY(0);
		}
		50% {
			transform: translateY(-5px);
		}
	}
	
	/* Dots grid background para estética cyberpunk */
	:global(body) {
		position: relative;
	}
	
	:global(body)::before {
		content: '';
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background-image: 
			radial-gradient(circle, rgba(var(--primary-rgb), 0.1) 1px, transparent 1px),
			radial-gradient(circle, rgba(var(--accent-rgb), 0.1) 1px, transparent 1px);
		background-size: 30px 30px, 25px 25px;
		background-position: 0 0, 15px 15px;
		z-index: -1;
		opacity: 0.3;
		pointer-events: none;
	}
	
	/* Highlight effect on hover */
	.hero-title:hover {
		transform: translateZ(5px) scale(1.05);
		filter: drop-shadow(0 0 20px rgba(var(--accent-rgb), 0.8));
	}
	
	/* Add reflective floor effect */
	.hero-section::before {
		content: '';
		position: absolute;
		height: 40px;
		bottom: 40px;
		left: 0;
		right: 0;
		background: linear-gradient(to bottom, 
			rgba(var(--primary-rgb), 0.1), 
			rgba(var(--accent-rgb), 0.05)
		);
		filter: blur(10px);
		transform: scaleY(-1);
		opacity: 0.3;
		z-index: -1;
	}
</style>
